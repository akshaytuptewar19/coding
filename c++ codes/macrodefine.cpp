#include <iostream>
//#include<bits/stdc++.h>
//#define ak 5
//#define area(l,b) (l*b)
#define min(a, b) (((a) < (b)) ? (a) : (b))
using namespace std;

int main() {
    int a=8,b=9;
    int n=8,q=7;
   // cout<<ak;
  // int m=area(n,q);

   cout<<min(a,b);
    
}

    